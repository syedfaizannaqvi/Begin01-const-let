{
  "name": "begin01-const-let",
  "version": "1.0.0",
  "description": "A simple program with const and let use into typescript and nodejs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "-": "^0.0.1",
    "@types/node": "^20.11.30"
  }
}
